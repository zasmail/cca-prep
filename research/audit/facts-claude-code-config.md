# Claude Code Configuration — Ground Truth Fact Sheet

Compiled 2026-07-16. All facts sourced from official `code.claude.com` docs (fetched live via `curl` after WebFetch's built-in redirect resolver returned a spurious `dns.google` redirect for this domain — that redirect was not followed; raw HTML was pulled directly with `curl` instead and confirmed to be genuine Anthropic/Mintlify-hosted content, ~1.5-4.5MB pages, `server: cloudflare`, `x-mintlify-client-version` headers, etc.). Local cached copies: `research/audit/raw-*.html` and `*-clean.txt` in this same directory.

Confidence key: **confirmed** = page fetched and text read directly. **inferred** = stated elsewhere on an official page and reasonably implies this claim but not verbatim-quoted here.

---

## 1. CLAUDE.md hierarchy and precedence

| # | Claim | Source | Quote | Confidence |
|---|-------|--------|-------|------------|
| 1 | CLAUDE.md load order, broadest→narrowest: Managed policy → User (`~/.claude/CLAUDE.md`) → Project (`./CLAUDE.md` or `./.claude/CLAUDE.md`) → Local (`./CLAUDE.local.md`) | https://code.claude.com/docs/en/memory | "The table below lists them in load order, from broadest scope to most specific, so a project instruction appears in context after a user instruction." | confirmed |
| 2 | Managed policy file paths: macOS `/Library/Application Support/ClaudeCode/CLAUDE.md`; Linux/WSL `/etc/claude-code/CLAUDE.md`; Windows `C:\Program Files\ClaudeCode\CLAUDE.md` | same | "macOS: /Library/Application Support/ClaudeCode/CLAUDE.md • Linux and WSL: /etc/claude-code/CLAUDE.md • Windows: C:\Program Files\ClaudeCode\CLAUDE.md" | confirmed |
| 3 | Managed CLAUDE.md "loads before user and project CLAUDE.md" and cannot be excluded by individual settings | same | "Precedence: same as a managed CLAUDE.md file. Loads before user and project CLAUDE.md." / "This file cannot be excluded by individual settings." | confirmed |
| 4 | Files are **concatenated** into context, not override-replaced; ordered filesystem-root → working directory, so project-level content is read *after* (and thus takes practical precedence in the model's attention over) user-level content | same | "All discovered files are concatenated into context rather than overriding each other. Across the directory tree, content is ordered from the filesystem root down to your working directory... instructions closer to where you launched Claude are read last." | confirmed |
| 5 | Within a directory, `CLAUDE.local.md` is appended after `CLAUDE.md` | same | "Within each directory, CLAUDE.local.md is appended after CLAUDE.md, so your personal notes are the last thing Claude reads at that level." | confirmed |
| 6 | Files in the directory hierarchy *above* the working directory load in full at launch; files in *subdirectories* below cwd load on demand when Claude reads files there | same | "CLAUDE.md and CLAUDE.local.md files in the directory hierarchy above the working directory are loaded in full at launch. Files in subdirectories load on demand when Claude reads files in those directories." | confirmed |
| 7 | CLAUDE.md content is delivered as a **user message after the system prompt**, not part of the system prompt — it's context, not enforced config | same | "CLAUDE.md content is delivered as a user message after the system prompt, not as part of the system prompt itself." | confirmed |
| 8 | Recommended size: under 200 lines per CLAUDE.md file; longer files reduce adherence | same | "Size: target under 200 lines per CLAUDE.md file. Longer files consume more context and reduce adherence." | confirmed |
| 9 | User-level rules (`~/.claude/rules/`) load *before* project rules, so project rules have higher priority | same | "User-level rules are loaded before project rules, giving project rules higher priority." | confirmed |
| 10 | `claudeMdExcludes` setting can exclude ancestor CLAUDE.md files by glob, at any layer (user/project/local/managed); managed-policy CLAUDE.md files cannot be excluded | same | "The claudeMdExcludes setting lets you skip specific files by path or glob pattern... Managed policy CLAUDE.md files cannot be excluded." | confirmed |
| 11 | Project-root CLAUDE.md survives `/compact` (re-read from disk and re-injected); nested subdirectory CLAUDE.md files are not automatically re-injected after compaction | same | "Project-root CLAUDE.md survives compaction: after /compact, Claude re-reads it from disk and re-injects it into the session. Nested CLAUDE.md files in subdirectories are not re-injected automatically." | confirmed |
| 12 | Block-level HTML comments in CLAUDE.md are stripped before injection into context (visible again via the Read tool) | same | "Block-level HTML comments (<!-- maintainer notes -->) in CLAUDE.md files are stripped before the content is injected into Claude's context." | confirmed |
| 13 | Claude Code reads **CLAUDE.md, not AGENTS.md**; teams using AGENTS.md should have CLAUDE.md `@import` it | same | "Claude Code reads CLAUDE.md, not AGENTS.md." | confirmed |

## 2. `@import` syntax

| # | Claim | Source | Quote | Confidence |
|---|-------|--------|-------|------------|
| 14 | Import syntax is `@path/to/file`; both relative and absolute paths allowed; relative paths resolve relative to the *importing file*, not cwd | https://code.claude.com/docs/en/memory | "CLAUDE.md files can import additional files using @path/to/import syntax... Both relative and absolute paths are allowed. Relative paths resolve relative to the file containing the import, not the working directory." | confirmed |
| 15 | Max recursive import depth is **4 hops** | same | "Imported files can recursively import other files, with a maximum depth of four hops." | confirmed |
| 16 | Import parsing skips markdown code spans/fenced code blocks — wrap a path in backticks (`` `@README` ``) to mention it literally without importing | same | "Import parsing skips Markdown code spans and fenced code blocks... writing `@README` keeps the text literal, while @README outside backticks imports the file." | confirmed |
| 17 | First time external imports are encountered, Claude Code shows a one-time approval dialog listing the files; declining disables imports (dialog doesn't reappear) | same | "The first time Claude Code encounters external imports in a project, it shows an approval dialog listing the files. If you decline, the imports stay disabled and the dialog does not appear again." | confirmed |
| 18 | Splitting content into `@`-imports helps organization but does **not** reduce context cost — imported files still load fully at launch | same | "You can also split content into imports for organization, though imported files still load and enter the context window at launch." | confirmed |

## 3. `.claude/rules/` behavior

| # | Claim | Source | Quote | Confidence |
|---|-------|--------|-------|------------|
| 19 | Rules live in `.claude/rules/`, one topic per file, discovered recursively (subfolders allowed) | https://code.claude.com/docs/en/memory | "Place markdown files in your project's .claude/rules/ directory... All .md files are discovered recursively" | confirmed |
| 20 | Rules **without** a `paths` frontmatter field load at launch with the same priority as `.claude/CLAUDE.md` | same | "Rules without paths frontmatter are loaded at launch with the same priority as .claude/CLAUDE.md." | confirmed |
| 21 | Path-scoped rules use YAML frontmatter `paths:` (glob list); they load only when Claude works with matching files, not on every tool call | same | "Rules can be scoped to specific files using YAML frontmatter with the paths field... Path-scoped rules trigger when Claude reads files matching the pattern, not on every tool use." | confirmed |
| 22 | Before Claude Code v2.1.211, project-excluded (`--setting-sources` excluding `project`) rules that load on demand (path-scoped/nested) still loaded incorrectly; fixed as of v2.1.211 | same | "Project rules are skipped if you exclude project from --setting-sources. Before v2.1.211, rules that load on demand... loaded even when project was excluded." | confirmed |
| 23 | `.claude/rules/` supports symlinks (including whole shared directories); circular symlinks are detected and handled | same | "The .claude/rules/ directory supports symlinks... circular symlinks are detected and handled gracefully." | confirmed |
| 24 | Glob bracket-expression bug: pre-v2.1.207, one invalid `paths` glob pattern (e.g. unescaped `[`) broke the Read tool for every file the rule was evaluated against; now it just matches nothing | same | "Before v2.1.207, one invalid pattern made the Read tool fail for every file the rule was evaluated against, instead of matching nothing." | confirmed |

## 4. Hooks — events and gating

Source for all hook facts below: https://code.claude.com/docs/en/hooks (confirmed, fetched directly).

| # | Claim | Quote | Confidence |
|---|-------|-------|------------|
| 25 | The **current full list** of hook events (far larger than the classic 8–9 taught in most prep material) is: SessionStart, Setup, UserPromptSubmit, UserPromptExpansion, PreToolUse, PermissionRequest, PermissionDenied, PostToolUse, PostToolUseFailure, PostToolBatch, Notification, MessageDisplay, SubagentStart, SubagentStop, TaskCreated, TaskCompleted, Stop, StopFailure, TeammateIdle, InstructionsLoaded, ConfigChange, CwdChanged, FileChanged, WorktreeCreate, WorktreeRemove, PreCompact, PostCompact, Elicitation, ElicitationResult, SessionEnd | Table: "EventWhen it fires SessionStart... Setup... UserPromptSubmit... UserPromptExpansion... PreToolUse... PermissionRequest... PermissionDenied... PostToolUse... PostToolUseFailure... PostToolBatch... Notification... MessageDisplay... SubagentStart... SubagentStop... TaskCreated... TaskCompleted... Stop... StopFailure... TeammateIdle... InstructionsLoaded... ConfigChange... CwdChanged... FileChanged... WorktreeCreate... WorktreeRemove... PreCompact... PostCompact... Elicitation... ElicitationResult... SessionEnd" | confirmed |
| 26 | Event cadence: SessionStart/SessionEnd = once per session; UserPromptSubmit/Stop/StopFailure = once per turn; PreToolUse/PostToolUse = every tool call | "Events fall into three cadences: once per session: SessionStart and SessionEnd; once per turn: UserPromptSubmit, Stop, and StopFailure; on every tool call inside the agentic loop: PreToolUse and PostToolUse." | confirmed |
| 27 | Exit code 0 = success, stdout parsed as JSON on exit 0 only; for most events stdout goes only to debug log — exceptions where stdout becomes visible context are **UserPromptSubmit, UserPromptExpansion, and SessionStart** | "Exit 0 means success... For most events, stdout is written to the debug log but not shown in the transcript. The exceptions are UserPromptSubmit, UserPromptExpansion, and SessionStart, where stdout is added as context that Claude can see and act on." | confirmed |
| 28 | Exit code 2 = blocking error; stdout/JSON ignored, stderr fed back to Claude as an error message (effect depends on event) | "Exit 2 means a blocking error. Claude Code ignores stdout and any JSON in it. Instead, stderr text is fed back to Claude as an error message." | confirmed |
| 29 | Exit code 1 (or any non-2 non-zero code) is a **non-blocking** error for most events — contradicts the common assumption that "any nonzero" blocks; only exit 2 blocks (exception: WorktreeCreate, where any non-zero code aborts) | "Claude Code treats exit code 1 as a non-blocking error and proceeds with the action, even though 1 is the conventional Unix failure code... The exception is WorktreeCreate, where any non-zero exit code aborts worktree creation." | confirmed |
| 30 | Per-event exit-code-2 behavior table confirms PreToolUse "blocks the tool call"; PostToolUse "cannot block — shows stderr to Claude, tool already ran"; Stop "prevents Claude from stopping, continues the conversation"; PermissionRequest "denies the permission"; several events (Notification, SessionStart, SubagentStart, SessionEnd, CwdChanged, FileChanged, PostCompact) have **no blocking capability** at all — "Shows stderr to user only" | Full table under "Exit code 2 behavior per event" | confirmed |
| 31 | JSON decision control: `decision: "block"` (top-level) used by UserPromptSubmit, UserPromptExpansion, PostToolUse, PostToolUseFailure, PostToolBatch, Stop, SubagentStop, ConfigChange, PreCompact. PreToolUse instead uses `hookSpecificOutput.permissionDecision` ("allow"/"deny"/"ask"/"defer") | "Uses hookSpecificOutput for richer control: allow, deny, or escalate to the user." / decision-pattern table | confirmed |
| 32 | `continue: false` in JSON output stops Claude entirely regardless of event type, taking precedence over any event-specific decision field | "continue... If false, Claude stops processing entirely after the hook runs. Takes precedence over any event-specific decision fields" | confirmed |
| 33 | Hook config nests: hook event → matcher group → hook handler(s); defined in `~/.claude/settings.json` (all projects, not shareable), `.claude/settings.json` (shareable), `.claude/settings.local.json` (gitignored), managed policy settings, plugin `hooks/hooks.json`, or Skill/agent frontmatter | "Hooks are defined in JSON settings files. The configuration has three levels of nesting..." + Hook locations table | confirmed |
| 34 | Five hook handler types exist: `command`, `http`, `mcp_tool`, `prompt` (single-turn LLM eval), `agent` (experimental, spawns a subagent with tools) | "There are five types: Command hooks... HTTP hooks... MCP tool hooks... Prompt hooks... Agent hooks (experimental and may change)" | confirmed |
| 35 | `additionalContext` (inside `hookSpecificOutput`) injects a system-reminder string into Claude's context at the point the hook fired — this is the documented mechanism for hooks to give Claude information, separate from blocking | "The additionalContext field passes a string from your hook into Claude's context window." | confirmed |
| 36 | Enterprise administrators can restrict hook sources via `allowManagedHooksOnly`, which blocks user/project/plugin hooks except plugins force-enabled via managed `enabledPlugins` | "Enterprise administrators can use allowManagedHooksOnly to block user, project, and plugin hooks." | confirmed |

## 5. Slash commands & Skills (now merged)

Source: https://code.claude.com/docs/en/skills (confirmed). **The dedicated "slash-commands" doc page no longer exists as a separate topic** — `code.claude.com/docs/en/slash-commands` now serves the Skills page content (title "Extend Claude with skills"). This is itself a key deprecation/consolidation fact.

| # | Claim | Quote | Confidence |
|---|-------|-------|------------|
| 37 | Custom commands (`.claude/commands/*.md`) have been **merged into skills**; a legacy commands file and a `SKILL.md` both create the same `/name` command and behave the same way. Old `.claude/commands/` files still work | "Custom commands have been merged into skills. A file at .claude/commands/deploy.md and a skill at .claude/skills/deploy/SKILL.md both create /deploy and work the same way. Your existing .claude/commands/ files keep working." | confirmed |
| 38 | Changelog explicitly documents this as a naming/mental-model consolidation with no behavior change | changelog: "Merged slash commands and skills, simplifying the mental model with no change in behavior" | confirmed |
| 39 | Skill file = directory + required `SKILL.md`, YAML frontmatter (`---`) + markdown body; only `description` is recommended (all fields optional) | "Every skill needs a SKILL.md file with two parts: YAML frontmatter... and markdown content" / "All fields are optional. Only description is recommended" | confirmed |
| 40 | Progressive disclosure: skill **description** is always loaded into context (so Claude knows it exists); full skill body loads only when invoked; supporting files (reference docs, scripts, templates) load on demand and cost near-zero until referenced | "Unlike CLAUDE.md content, a skill's body loads only when it's used, so long reference material costs almost nothing until you need it." / "This keeps SKILL.md focused on the essentials while letting Claude access detailed reference material only when needed." | confirmed |
| 41 | Skill locations by scope: Enterprise (managed settings) > Personal `~/.claude/skills/<name>/SKILL.md` > Project `.claude/skills/<name>/SKILL.md` > Plugin `<plugin>/skills/<name>/SKILL.md`. Same-name collision: enterprise overrides personal, personal overrides project; any of these overrides a same-named bundled skill | "When skills share the same name across levels, enterprise overrides personal, and personal overrides project." | confirmed |
| 42 | Skills also load from nested `.claude/skills/` in subdirectories (monorepo support); name clashes get directory-qualified names like `apps/web:deploy` | "Skills also load from nested .claude/skills/ directories below your working directory." / "The nested one appears under a directory-qualified name, apps/web:deploy." | confirmed |
| 43 | Key frontmatter fields: `name`, `description`, `when_to_use`, `argument-hint`, `arguments`, `disable-model-invocation`, `user-invocable`, `allowed-tools`, `disallowed-tools`, `model`, `effort`, `context` (`fork`), `agent`, `hooks`, `paths`, `shell` | Full frontmatter reference table | confirmed |
| 44 | `disable-model-invocation: true` = only the user can invoke (`/name`), Claude cannot auto-trigger it; `user-invocable: false` = only Claude can invoke it, hidden from `/` menu | "disable-model-invocation: true: Only you can invoke the skill... user-invocable: false: Only Claude can invoke the skill." | confirmed |
| 45 | Bundled skills ship with Claude Code itself (e.g. `/doctor`, `/code-review`, `/batch`, `/debug`, `/loop`, `/claude-api`) and can be disabled via `disableBundledSkills` setting | "Claude Code includes a set of bundled skills that are available in every session unless disabled with the disableBundledSkills setting, including /doctor, /code-review, /batch, /debug, /loop, and /claude-api." | confirmed |
| 46 | `$ARGUMENTS`, `$ARGUMENTS[N]`, `$N` (shorthand), `$name` (named arg from `arguments:` frontmatter), plus `${CLAUDE_SESSION_ID}`, `${CLAUDE_EFFORT}`, `${CLAUDE_SKILL_DIR}`, `${CLAUDE_PROJECT_DIR}` are supported substitutions | Substitutions table | confirmed |
| 47 | Claude Code Skills follow the **Agent Skills open standard** (cross-tool), with Claude Code adding extra features (invocation control, subagent execution, dynamic context injection) | "Claude Code skills follow the Agent Skills open standard, which works across multiple AI tools." | confirmed |
| 48 | Recommend keeping `SKILL.md` under 500 lines; move detail to supporting files | "Keep SKILL.md under 500 lines. Move detailed reference material to separate files." | confirmed |

## 6. Subagents / agent definitions

Source: https://code.claude.com/docs/en/sub-agents (confirmed).

| # | Claim | Quote | Confidence |
|---|-------|-------|------------|
| 49 | Subagent file = YAML frontmatter + markdown system-prompt body; only `name` and `description` required | "Only name and description are required." | confirmed |
| 50 | Full frontmatter field list: `name`, `description`, `tools`, `disallowedTools`, `model`, `permissionMode`, `maxTurns`, `skills`, `mcpServers`, `hooks`, `memory`, `background`, `effort`, `isolation`, `color`, `initialPrompt` | Full frontmatter table | confirmed |
| 51 | Scope precedence (highest→lowest): Managed settings (org-wide) > `--agents` CLI flag (session-only) > `.claude/agents/` (project) > `~/.claude/agents/` (user) > plugin `agents/` directory | Priority table 1–5 | confirmed |
| 52 | Built-in subagents: **Explore** (read-only, fast, model inherits from main convo capped at Opus on the Claude API), **Plan** (research during plan mode, read-only), **general-purpose** (all tools, complex multi-step work) | Built-in subagents section | confirmed |
| 53 | `model` field accepts alias (`sonnet`/`opus`/`haiku`/`fable`), full model ID, or `inherit` (default); subagents as of v2.1.198 also inherit the main conversation's extended-thinking on/off state (no per-subagent thinking setting) | "As of v2.1.198, subagents also inherit the main conversation's extended thinking configuration" | confirmed |
| 54 | Subagents receive only their own system prompt + basic environment details (cwd, etc.) — **not** the full Claude Code system prompt | "Subagents receive only this system prompt plus basic environment details like the working directory, not the full Claude Code system prompt." | confirmed |
| 55 | Plugin-sourced subagents do **not** support `hooks`, `mcpServers`, or `permissionMode` frontmatter fields (ignored for security reasons) | "For security reasons, plugin subagents don't support the hooks, mcpServers, or permissionMode frontmatter fields." | confirmed |

## 7. Plugins

Source: https://code.claude.com/docs/en/plugins (confirmed).

| # | Claim | Quote | Confidence |
|---|-------|-------|------------|
| 56 | A plugin is a self-contained directory, optionally with a `.claude-plugin/plugin.json` manifest, that bundles skills, agents, hooks, and/or MCP servers, shareable across projects/teams | "Plugins let you extend Claude Code with custom functionality that can be shared across projects and teams." | confirmed |
| 57 | `plugin.json` required-ish fields: `name` (unique id + skill namespace), `description`; optional `version` (controls update semantics — if omitted and distributed via git, commit SHA = version, every commit = new version), `author` | Manifest field table | confirmed |
| 58 | Plugin directory layout: `.claude-plugin/` (manifest ONLY — never put `commands/`, `agents/`, `skills/`, `hooks/` inside it), `skills/`, `commands/` (legacy flat-file skills), `agents/`, `hooks/` (with `hooks.json`), `.mcp.json`, `.lsp.json`, `monitors/`, `bin/` (added to Bash PATH), `settings.json` (default settings when plugin enabled) — all these live at the plugin **root**, not inside `.claude-plugin/` | "Common mistake: Don't put commands/, agents/, skills/, or hooks/ inside the .claude-plugin/ directory. Only plugin.json goes inside .claude-plugin/." + directory table | confirmed |
| 59 | Plugin skills are always namespaced as `/plugin-name:skill-name` to prevent collisions between plugins | "Why namespacing? Plugin skills are always namespaced (like /my-first-plugin:hello) to prevent conflicts when multiple plugins have skills with the same name." | confirmed |
| 60 | Test a local plugin with `claude --plugin-dir ./my-plugin`; `claude plugin init <name>` scaffolds one into `~/.claude/skills/<name>/` that auto-loads as `<name>@skills-dir` with no marketplace/install step | "Run Claude Code with the --plugin-dir flag to load your plugin" / "claude plugin init scaffolds one... it loads as my-tool@skills-dir with no marketplace or install step." | confirmed |

## 8. `settings.json` and permissions

Source: https://code.claude.com/docs/en/settings (confirmed).

| # | Claim | Quote | Confidence |
|---|-------|-------|------------|
| 61 | Four config scopes: Managed (server/plist/registry/managed-settings.json, org-wide, highest) > CLI args (session-only override) > Local (`.claude/settings.local.json`) > Project (`.claude/settings.json`) > User (`~/.claude/settings.json`, lowest, applies when nothing else specifies it) | "When the same setting appears in multiple scopes, Claude Code applies them in priority order: Managed (highest)... Command line arguments... Local... Project... User (lowest)" | confirmed |
| 62 | Permission rules are the **exception** to override-based precedence — they **merge** across scopes rather than one scope overriding another | "Permission rules behave differently because they merge across scopes rather than override." | confirmed |
| 63 | Permission keys: `allow`, `ask`, `deny`, `additionalDirectories`, `defaultMode` — rule format is `Tool` or `Tool(specifier)` | Permission settings table | confirmed |
| 64 | Rule evaluation order is fixed: **deny rules first, then ask, then allow**; first match wins regardless of specificity | "Rules are evaluated in order: deny rules first, then ask, then allow. The first match determines the outcome regardless of rule specificity." | confirmed |
| 65 | `defaultMode` valid values: `default`, `acceptEdits`, `plan`, `auto`, `dontAsk`, `bypassPermissions`, and `manual` (alias for `default`, requires v2.1.200+); `auto` is ignored if set in project/local settings (can only be set in `~/.claude/settings.json`) so a repo can't grant itself auto mode (changed as of v2.1.142 — before that, project settings *could* set `auto`) | "auto is ignored when set in project or local settings, so a repository can't grant itself auto mode; set it in ~/.claude/settings.json instead. Before v2.1.142, project settings could set auto." | confirmed |
| 66 | `disableBypassPermissionsModeSet` to `"disable"` prevents `--dangerously-skip-permissions`/bypassPermissions mode entirely (org policy enforcement) | Global config settings table | confirmed |
| 67 | Managed settings can be delivered via: server-managed (claude.ai admin console or self-hosted gateway), macOS MDM plist (`com.anthropic.claudecode`), Windows registry (`HKLM\SOFTWARE\Policies\ClaudeCode`, or `HKCU` at lowest policy priority), or file-based `managed-settings.json`/`managed-mcp.json` at OS-specific system paths, with an optional `managed-settings.d/` drop-in directory merged systemd-style | Managed settings delivery section | confirmed |
| 68 | Legacy Windows managed-settings path `C:\ProgramData\ClaudeCode\managed-settings.json` is **no longer supported as of v2.1.75** — must migrate to `C:\Program Files\ClaudeCode\managed-settings.json` | "The legacy Windows path C:\ProgramData\ClaudeCode\managed-settings.json is no longer supported as of v2.1.75." | confirmed |
| 69 | Other config lives in `~/.claude.json` (OAuth session, user/local-scope MCP servers, per-project trust/allowed-tools state, caches); project-scoped MCP servers are in `.mcp.json` | "Other configuration is stored in ~/.claude.json... Project-scoped MCP servers are stored separately in .mcp.json." | confirmed |
| 70 | Sandbox settings (`sandbox.enabled`, `sandbox.filesystem.*`, `sandbox.network.*`, `sandbox.credentials.*`) isolate Bash commands from filesystem/network at the OS level, separate from (but mergeable with) tool-level permission rules | Sandbox settings section | confirmed |

## 9. Headless / non-interactive (`-p`/`--print`) mode

Source: https://code.claude.com/docs/en/headless (confirmed) and https://code.claude.com/docs/en/cli-reference (confirmed).

| # | Claim | Quote | Confidence |
|---|-------|-------|------------|
| 71 | Non-interactive invocation: `claude -p "prompt"` (alias `--print`); this is the CLI entry point to the Agent SDK | "To run Claude Code in non-interactive mode, pass -p with your prompt and any CLI options" | confirmed |
| 72 | `--bare` flag skips auto-discovery of hooks, skills, plugins, MCP servers, auto memory, and CLAUDE.md for faster/more reproducible CI runs; without it, `-p` loads the same context an interactive session would | "Add --bare to reduce startup time by skipping auto-discovery of hooks, skills, plugins, MCP servers, auto memory, and CLAUDE.md." | confirmed |
| 73 | `--bare` is stated to become the **default for `-p` in a future release** | "--bare is the recommended mode for scripted and SDK calls, and will become the default for -p in a future release." | confirmed |
| 74 | `--output-format` options: `text` (default), `json` (structured, includes `result`, session ID, `total_cost_usd`, per-model cost breakdown), `stream-json` (newline-delimited streaming) | "text (default): plain text output / json: structured JSON with result, session ID, and metadata / stream-json: newline-delimited JSON for real-time streaming" | confirmed |
| 75 | `--input-format` options: `text`, `stream-json` (print mode only) | cli-reference: "Specify input format for print mode (options: text, stream-json)" | confirmed |
| 76 | `--json-schema` + `--output-format json` validates structured output against a JSON Schema; invalid schema errors out (as of v2.1.205 — before that it silently returned unstructured text) | "Claude Code exits with an error on an invalid schema and accepts the format keyword as an annotation without client-side validation. Before v2.1.205, an invalid schema produced unstructured output with no error" | confirmed |
| 77 | `--permission-mode` accepts `default`, `acceptEdits`, `plan`, `auto`, `dontAsk`, `bypassPermissions`, or `manual` (alias for `default`, v2.1.200+); overrides `defaultMode` from settings for that session | cli-reference `--permission-mode` entry | confirmed |
| 78 | `--dangerously-skip-permissions` = equivalent to `--permission-mode bypassPermissions` | cli-reference: "Skip permission prompts. Equivalent to --permission-mode bypassPermissions." | confirmed |
| 79 | `--max-turns` limits agentic turns in print mode; exits with error at limit; no limit by default | cli-reference `--max-turns` entry | confirmed |
| 80 | `--max-budget-usd` caps API spend before stopping (print mode only) | cli-reference `--max-budget-usd` entry | confirmed |
| 81 | Piped stdin is capped at **10MB** as of v2.1.128; exceeding it errors with non-zero exit | "As of Claude Code v2.1.128, piped stdin is capped at 10MB." | confirmed |
| 82 | A background Bash task started during `claude -p` is killed ~5 seconds after the final result (grace period), except background subagents/workflows which are awaited (capped at 10 minutes by default as of v2.1.182, adjustable via `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS`) | Background tasks at exit section | confirmed |
| 83 | `--setting-sources` (comma-separated `user,project,local`) controls which settings scopes load for the session | cli-reference `--setting-sources` entry | confirmed |

## 10. Recently renamed / deprecated / removed (explicit changelog + docs findings)

Source: https://code.claude.com/docs/en/changelog (confirmed, fetched directly) unless noted.

| # | Claim | Quote | Confidence |
|---|-------|-------|------------|
| 84 | Custom slash commands and Skills were merged; `.claude/commands/*.md` is now the **legacy format**, `.claude/skills/<name>/SKILL.md` is the current/recommended format (old files still work) | changelog: "Merged slash commands and skills, simplifying the mental model with no change in behavior" + skills doc: "Custom commands have been merged into skills." | confirmed |
| 85 | `/simplify` was renamed to `/code-review`; old cleanup-and-fix behavior removed, replaced with correctness-bug reporting at a chosen effort level, with `--comment` for inline PR comments | "Renamed /simplify to /code-review. It now reports correctness bugs at a chosen effort level (e.g., /code-review high); pass --comment to post findings as inline GitHub PR comments. The old cleanup-and-fix behavior has been removed" | confirmed |
| 86 | `/output-style` command deprecated in favor of `/config` (output style now fixed at session start for prompt-caching reasons) | "Deprecated /output-style command — use /config instead. Output style is now fixed at session start for better prompt caching" | confirmed |
| 87 | Output styles feature itself has a deprecate/un-deprecate history: deprecated once (told to use `--system-prompt-file`, `--system-prompt`, `--append-system-prompt`, CLAUDE.md, or plugins instead), then **un-deprecated** later based on community feedback | "Deprecated output styles. Review options in /output-style and use —system-prompt-file, —system-prompt, —append-system-prompt, CLAUDE.md, or plugins instead" then later "Un-deprecate output styles based on community feedback" | confirmed |
| 88 | `/allowed-tools` renamed to `/permissions` | "Renamed /allowed-tools -> /permissions" | confirmed |
| 89 | `claude config` CLI commands are deprecated in favor of directly editing `settings.json` | "Deprecated claude config commands in favor of editing settings.json" | confirmed |
| 90 | MCP server scopes were renamed: former "project" scope is now **"local"**, former "global" scope is now **"user"** | "Previous MCP server scopes have been renamed: previous 'project' scope is now 'local' and 'global' scope is now 'user'" | confirmed |
| 91 | `--enable-auto-mode` flag was **removed in v2.1.111** — Auto mode is now in the Shift+Tab mode cycle by default; use `--permission-mode auto` instead | cli-reference: "--enable-auto-mode Removed in v2.1.111. Auto mode is now in the Shift+Tab cycle by default; use --permission-mode auto to start in it" | confirmed |
| 92 | `--remote` flag is a **deprecated alias for `--cloud`** | cli-reference: "--remote Deprecated alias for --cloud" | confirmed |
| 93 | Legacy Windows managed-settings path `C:\ProgramData\ClaudeCode\managed-settings.json` removed/no-longer-supported as of v2.1.75; must use `C:\Program Files\ClaudeCode\managed-settings.json` | "Breaking change: Removed deprecated Windows managed settings fallback at C:\ProgramData\ClaudeCode\managed-settings.json" | confirmed |
| 94 | `includeCoAuthoredBy` setting deprecated in favor of the newer `attribution` setting (commit/PR bylines) | "Added attribution setting to customize commit and PR bylines (deprecates includeCoAuthoredBy)" / settings doc: "The attribution setting takes precedence over the deprecated includeCoAuthoredBy setting." | confirmed |
| 95 | `.claude.json` top-level `allowedTools`, `ignorePatterns`, `env`, `todoFeatureEnabled` config keys removed — must configure via `settings.json` instead | "Remove deprecated .claude.json allowedTools, ignorePatterns, env, and todoFeatureEnabled config options (instead, configure these in your settings.json)" | confirmed |
| 96 | `TaskOutput` tool deprecated in favor of using `Read` on the background task's output file path | "Deprecated TaskOutput tool in favor of using Read on the background task's output file path" | confirmed |
| 97 | Tool renames for consistency: `LSTool` → `LS`, `View` → `Read`, etc. (older, historical) | "Renamed tools for consistency: LSTool -> LS, View -> Read, etc." | confirmed |
| 98 | SDK field rename: `total_cost` → `total_cost_usd` | "SDK: Renamed total_cost to total_cost_usd" | confirmed |
| 99 | `/fork` renamed to `/branch` (`/fork` still works as an alias) | "Renamed /fork to /branch (/fork still works as an alias)" | confirmed |
| 100 | "Extra usage" renamed to "usage credits"; `/extra-usage` is now `/usage-credits` (old name still works) | "Renamed 'extra usage' to 'usage credits' across CLI copy; /extra-usage is now /usage-credits (old name still works)" | confirmed |
| 101 | Windsurf renamed to Devin Desktop across `/ide` menu, `/terminal-setup`, `/scroll-speed` (following the editor's own rebrand, not an Anthropic product change) | "Renamed Windsurf to Devin Desktop in the /ide menu, /terminal-setup, and /scroll-speed, following the editor's rebrand" | confirmed |
| 102 | `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE` env var deprecated (scheduled removal 06/01) — use `/model claude-opus-4-6[1m]` then `/fast on` | "Deprecated CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE (will be removed on 06/01)." | confirmed |
| 103 | MCP wizard interface deprecated in favor of other MCP commands | "Deprecated MCP wizard interface in favor of other MCP commands" | confirmed |
| 104 | Piped input in interactive (non-`-p`) mode is deprecated | "Deprecated piped input in interactive mode" | confirmed |
| 105 | `ENABLE_PROMPT_CACHING_1H_BEDROCK` env var is deprecated but still honored, superseded by `ENABLE_PROMPT_CACHING_1H` | "ENABLE_PROMPT_CACHING_1H_BEDROCK is deprecated but still honored" | confirmed |

---

## Unverifiable

The following could not be confirmed on an official Anthropic domain within this pass (either the specific page/anchor wasn't reachable, or the exact figure/claim wasn't stated verbatim on a fetched page):

- **Exact current model context-window/output-token figures and per-token pricing** for Opus/Sonnet/Haiku 4.6-era models (e.g. the numbers in the project's own CLAUDE.md reference card: 1M/128K for Opus 4.6, $5/$25 per Mtok, etc.) — these are typically on `docs.claude.com`/`platform.claude.com` pricing and model pages, not the `code.claude.com` Claude Code docs, and were not fetched in this pass. Should be separately verified against `platform.claude.com/docs/en/about-claude/models` or the pricing page before treating as ground truth for the exam.
- **Prompt caching specifics** (10% cache-hit cost, 1.25x/2x write multipliers for 5-min/1-hour TTL, 1024-token minimum, 4 cache breakpoints, tools→system→messages prefix order) — these live on the Claude Developer Platform docs (`platform.claude.com` or `docs.claude.com`), not on the Claude Code docs pages fetched here; not independently re-verified in this pass.
- **Batch API limits** (10,000 requests/batch, 50% discount, 24-hour window, 29-day result retention) — same as above, API-platform facts outside the Claude Code doc set fetched.
- **Citations feature specifics** (cited_text not counted as output tokens, incompatible with Structured Outputs, ~15% recall improvement) — API-platform feature, not covered by the Claude Code docs fetched in this pass.
- **tool_choice `any`/`tool` incompatibility with extended thinking** — this is an Anthropic Messages API detail, not covered on the Claude Code config pages fetched; would need verification against `platform.claude.com/docs/en/build-with-claude/tool-use` or the extended-thinking docs.
- The **exact list of `stop_reason` values** including `model_context_window_exceeded` — this is an API response field, not covered in the Claude Code docs pages fetched (memory, hooks, skills, sub-agents, plugins, settings, headless, cli-reference, changelog). Needs verification against the Messages API reference.
- Whether **all 3 valid escalation triggers / 2 invalid trigger anti-patterns** (customer_request, policy_gap, capability_limit / sentiment-based, self-reported confidence) as named concepts appear verbatim on any official Anthropic page — not found in the Claude Code config docs fetched; this framing may be specific to Anthropic Academy/CCA-F exam training material rather than the product docs, and could not be confirmed against `anthropic.skilljar.com` in this pass (not fetched).
- Precise wording of the "5 orchestration patterns" (Prompt Chaining, Routing, Parallelization, Orchestrator-Workers, Evaluator-Optimizer) as it appears in "Building Effective Agents" — not verified in this pass since that content lives on `anthropic.com`/`docs.claude.com` engineering-blog pages outside the Claude Code config doc set fetched.
